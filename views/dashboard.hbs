<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        nav {
            background-color: orange;
            color: #fff;
            padding: 15px 60px;
            display: flex;
            justify-content: space-between;
        }

        nav h1 {
            font-size: 30px;
            margin: 0;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 30px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .btn-logout {
            background-color: orange;
            border: none;
            color: white;
            padding: 10px 20px;
            font-size: 18px;
            border-radius: 30px;
            cursor: pointer;
            transition: 0.3s;
        }

        .btn-logout:hover {
            background-color: #ff9f00; 
        }

        .welcome-message {
            font-size: 24px;
            margin-bottom: 30px;
            text-align: center;
            color: #333; 
        }

        .logo {
            font-size: 30px;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">
            <h1>My Website</h1>
        </div>
        <ul>
            <li><a href="/logout" class="btn-logout">Logout</a></li>
        </ul>
    </nav>

 
    <div class="container">
        <div class="welcome-message">
            <h2>Welcome, <strong>{{user.name}}</strong>!</h2>
            <p>You are successfully logged in and your session is active.</p>
        </div>

        <div class="text-center">
            <a href="/logout" class="btn btn-danger btn-logout">Logout</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0Wv8Fq8QXyowj6X6iA8BXS1qjj5Dq4hS5nftlHkDdO4+fsd" crossorigin="anonymous"></script>
</body>
</html>



